// 统一 z 层级（语义化，集中管理——避免散落的魔法值彼此盖来盖去）。
// 值为 Tailwind 类字符串：Tailwind 内容扫描会在本文件看到这些字面量并生成对应类。
// 顺序（低→高）：面板 < 模态 < 全屏工具 < 右键菜单 < 对话框 < 下拉浮层 < 通知 < 气泡提示。
// 关键：dropdown 必须高于 modal 与 dialog——否则模态/对话框内打开的 <Select> 会被遮挡。
export const Z = {
  panel: 'z-[60]', // 侧浮层 / 设置面板 / 灯箱
  modal: 'z-[80]', // 常规模态 + 全屏编辑器
  fullscreen: 'z-[90]', // 导演台 / 360 全景
  contextMenu: 'z-[95]', // 右键菜单
  dialog: 'z-[120]', // 确认 / 输入对话框（在模态之上）
  dropdown: 'z-[150]', // Select / 菜单浮层（必须高于模态与对话框）
  toast: 'z-[200]', // 通知
  tooltip: 'z-[300]' // 气泡提示（最高）
}
