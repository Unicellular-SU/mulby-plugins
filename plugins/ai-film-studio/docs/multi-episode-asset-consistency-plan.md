# AI Film Studio 多集短剧与资产一致性优化方案

## 背景

当前 `ProjectDoc` 更像“一集项目”：`scripts`、`storyboards`、`clips`、`track` 都直接挂在项目顶层。资产虽然是项目级数组，并且已经有 `AssetVariant` 字段，但分镜只保存 `associateAssetIds`，只能表达“这个镜头出现了谁”，不能表达“这个镜头使用这个人的哪套妆容、服装、年龄阶段或受伤状态”。

短剧多集化后，这会带来两个直接问题：

- 多集内容会互相覆盖或混在同一条分镜时间线里，难以单集生成、复查、重剪和成片。
- 同一角色的不同形态没有端到端引用，后续关键帧和视频只能拿角色主图，容易把“少年/成年/老年”“常服/战损/礼服”“淡妆/浓妆”等状态混成同一张参考图。

目标是把项目升级成“系列工作台”：项目保存全局设定和资产池；每一集保存自己的剧本、分镜、视频轨和成片；分镜用结构化 cast 引用锁定具体资产变体。

## 当前落地状态

截至 2026-07-06，核心多集路径已经落地：

- `ProjectDoc.episodes` 与 `currentEpisodeId` 已接入，顶层 `scripts/storyboards/clips/track/storyboardTable` 作为当前集兼容镜像保留。
- Studio 顶部已有 Episode 切换器，支持新建、批量创建、重命名、删除和切换剧集。
- 原著章节可分配到不同剧集，并能按章节顺序重新均分；连续性检查会标记未分配、重复分配和无效章节引用。
- `Asset.variants[]` 已用于同一角色的妆容、服装、时期、受伤状态等形态，变体可标记适用剧集。
- `Asset.aliases[]` 已用于同一资产的别名、昵称、称号或常见误写；资产页、Agent 资产写入、项目搜索和分镜出场匹配都会用名称/别名复用既有角色、场景或道具，降低跨集重复建档概率。
- 分镜通过 `castRefs` 精确引用资产和变体；关键帧、视频参考图收集、提示词编号和连续性检查都从同一引用模型派生。
- 生成前置检查会阻止缺失资产、缺失变体、缺失参考图或附件二进制丢失的分镜继续生成，即使当前视频供应商不支持多参考图也不会绕过资产一致性校验。
- 资产页已有跨集资产矩阵，分镜页已有一致性详情抽屉；连续性报告会标记项目级角色/场景/道具未被任何分镜引用、同类型资产重名和名称/别名冲突、同一 `sceneId` 连续场景漏挂或混用场景资产、同场景角色形态漂移、前集已用形态在后续集回退主形象、章节分配、变体适用范围、本集专属形态未绑定和缺失参考图，并提供生成缺失参考图、标记变体适用本集/场景/分镜、绑定唯一候选形态、选择并绑定多候选形态、补齐/统一同场景资产、统一同场景角色形态、沿用上一形态、重新均分章节等快速修复。
- 顶栏已有「生成全剧」入口，可按剧集顺序逐集执行资产、被分镜引用的形态参考图、关键帧、视频和合成；已成片、失败和暂缓剧集默认跳过，运行中可请求暂停后续剧集并在当前集完成后停止，每集成片路径写回 `Episode.filmPath`，多集输出文件名包含集号和集标题。
- 每集合成成功后会生成 `Episode.productionRecap`，从本集剧本、分镜、出场资产/变体和视频片段进度提炼制作回顾；Agent 的剧集上下文和工具读取都会暴露该回顾，供后续集承接人物状态和剧情进度。
- Episode 切换器已有跨集承接入口，会主动汇总最近制作回顾，以及当前集资产/形态在其他剧集中的出现记录；当前集还没有分镜出场记录时，会从最近上一集给出可承接资产/形态线索；还会给出补参考图、标记形态适用本集、新建并应用本集形态等建议，可一键顺序处理可执行项。当前集回到主形象但上一相关剧集用过具体形态时，建议会明确提示状态回退，并基于上一形态预填本集形态提示词。Agent 也可用 `get_episode_handoff` 读取同一套线索和建议；分阶段分镜 Agent 和工具循环都会在续写、换装、妆容、受伤或时期变化任务中优先绑定 `castRefs.variantLabel/variantId`，避免生成时回退主形象。
- 顶栏已有「导出全季」入口，会把已成片剧集复制到用户选择的目录，为每集生成旁挂 SRT，并写入 `manifest.json` 记录集号、标题、源路径、导出路径、字幕文件、素材引用表和缺失项报告。
- Episode 切换器已有「导出当前集」入口，会把当前集成片和旁挂 SRT 复制到用户选择的目录，并写入 `episode.json` 记录本集剧本、分镜、制作回顾、导出路径、字幕文件、素材引用表和缺失项报告。
- Episode 切换器已有重置当前集成片状态入口，可清理 `filmPath/filmError/producedAt/productionRecap`，让已成片或失败剧集重新进入「生成全剧」队列；也可把未准备好的剧集标记为暂缓，之后恢复入队。

后续重点不再是“能不能表达多集”，而是跨集记忆召回的自动应用和更深的自动化修复。

## Toonflow 启发

Toonflow 的公开代码里有几个值得借鉴的结构思路：

- 资产挂 `projectId`，形成项目级资产池，而不是每个镜头临时复制一份资产。
- 剧本、分镜、视频和 Agent 工作数据大量使用 `scriptId` 做工作范围隔离，接近“每集一条内容线”的模型。
- `episodesId` 字段已经出现在脚本相关记录里，说明它至少预留了剧集层级。
- `assetsId` 作为资产自引用，可表达派生资产、换装、状态图等从属关系。

本插件不需要复制 Toonflow 的表结构；在 Mulby 插件里仍保持 `ProjectDoc` JSON KV 的持久化方式。但数据边界应该对齐这些原则：项目级资产共享，剧集级内容隔离，资产变体可被镜头精确引用。

## 目标模型

### 1. Project 是系列容器

项目顶层保留跨集共享数据：

- `meta`：项目名、画风、比例、默认模型、导演手册等。
- `novel`：可作为全集原著/章节池。
- `assets`：全局角色、场景、道具、音色、素材片段。
- `memory`：可按项目、剧集或 Agent 维度继续隔离。
- `episodes`：每一集的剧本、分镜、视频轨、成片状态。
- `currentEpisodeId`：当前正在编辑和生成的一集。

### 2. Episode 是单集工作线

新增 `Episode`，承载现在顶层那些单集专属内容：

```ts
export interface Episode {
  id: string
  index: number
  title: string
  summary?: string
  productionRecap?: string
  seriesSkip?: boolean
  status?: 'draft' | 'planned' | 'generating' | 'done'
  filmPath?: string
  filmError?: string
  producedAt?: number
  novelChapterIds?: string[]
  scripts: Script[]
  storyboards: Storyboard[]
  storyboardTable?: StoryboardTableScene[]
  clips: Clip[]
  track: VideoTrack[]
  createdAt: number
  updatedAt: number
}
```

第一阶段保持旧字段兼容：顶层 `scripts/storyboards/clips/track/storyboardTable` 仍作为当前集的兼容镜像，旧 UI 和生成逻辑可以逐步迁移。迁移完成后，新的读写入口统一走当前 `Episode`。

### 3. Asset 是跨集身份，Variant 是具体形态

角色资产表示“同一个人”的稳定身份；变体表示某一集、某个场景或某个状态下的具体外观。

资产可维护 `aliases?: string[]`，用于记录同一身份的别名、昵称、称号、旧名或常见写法。别名只解决“是不是同一个资产”的召回问题，不承载妆容、服装、年龄阶段等状态变化；这些仍应进入 `AssetVariant`。

推荐扩展 `AssetVariant`：

```ts
export interface AssetVariant {
  id: string
  label: string
  desc?: string
  prompt?: string
  refImageId?: string
  parentVariantId?: string
  appliesToEpisodeIds?: string[]
  appliesToSceneIds?: string[]
  appliesToStoryboardIds?: string[]
  tags?: string[]
  state?: GenState
  error?: string
}
```

示例：

- `role: 林砚` 是同一角色身份。
- `variant: 第一集-日常淡妆`、`variant: 第二集-战损妆`、`variant: 回忆-少年期` 是不同形态。
- 角色主图作为默认形象；分镜指定 `variantId` 时优先使用变体图。

### 4. Storyboard 用 castRefs 锁定资产变体

保留 `associateAssetIds` 做兼容，但新增结构化引用：

```ts
export interface StoryboardCastRef {
  assetId: string
  variantId?: string
  roleInShot?: 'lead' | 'supporting' | 'background'
  note?: string
}
```

`Storyboard.castRefs?: StoryboardCastRef[]` 成为后续生成的权威输入。旧项目读取时，如果没有 `castRefs`，由 `associateAssetIds` 自动回退为 `{ assetId }`。

关键帧、视频提示词和参考图收集必须从同一份 `castRefs` 派生，避免提示词里写了 `@图2`，实际发给供应商的第二张图却是另一张。

## 多集工作流

### 系列规划

用户导入故事或输入短剧概念后，Agent 先生成系列级规划：

- 总梗概。
- 角色表和稳定身份。
- 跨集场景、道具、音色。
- 集数列表：标题、集梗概、冲突点、主要出场角色。

这一步只创建项目级资产草稿和 `episodes` 骨架，不立即把所有集的分镜都生成出来。

### 单集生产

用户选择某一集后，工作流保持现在的熟悉顺序：

`本集剧情 -> 本集剧本 -> 本集分镜 -> 关键帧 -> 视频候选 -> 选优 -> 合成`

差异是所有单集产物写入当前 `Episode`。资产仍从项目全局池读取，并在需要时创建或选择变体。

### 资产一致性守门

生成关键帧或视频前做一次轻量预检：

- 分镜里的 `assetId` 是否存在。
- 指定 `variantId` 是否属于该资产。
- 有 `variantId` 时是否已有 `refImageId`。
- 无 `variantId` 时是否允许回退主图。
- 同一镜头中是否出现同名但不同 `assetId` 的角色。

预检结果应显示在 UI 或 Agent 回复里，阻止“静默错图”。

## 分阶段落地计划

### P0：数据边界与兼容迁移

- 新增 `Episode` 类型。
- `ProjectDoc` 新增 `episodes?: Episode[]` 和 `currentEpisodeId?: string`。
- `normalizeDoc` 对旧项目自动创建“第 1 集”，把当前顶层 `scripts/storyboards/clips/track/storyboardTable` 复制进该集。
- 暂时保留顶层字段作为当前集镜像，避免一次性重写全部 UI 和 store。
- `ProjectCard` 增加可选 `episodeCount`，首页可逐步显示集数。

验收：旧项目能正常打开；新项目默认带一集；构建通过；没有改变现有单集生成行为。

### P0：castRefs 与变体引用

- 新增 `StoryboardCastRef`。
- `Storyboard` 新增 `episodeId?` 和 `castRefs?`。
- 增加 helper：`castRefsForStoryboard(sb)`，没有 `castRefs` 时回退 `associateAssetIds`。
- 参考图收集、视频提示词编号、关键帧提示词逐步改为使用同一 helper。
- `AssetVariant` 增加适用范围字段，但保持旧字段可读。

验收：旧分镜仍能生成；新分镜可以锁定某个角色变体；提示词编号和实际发送参考图一致。

### P1：剧集 UI 与工作区操作

- 在 Studio 顶部增加 Episode 切换器。
- 支持新建、重命名、复制、删除剧集。
- 切换剧集时同步当前集镜像，现有脚本、分镜、时间线视图展示当前集。
- 首页卡片显示 `episodeCount` 和当前集进度。

验收：用户可以在同一项目中维护多集，每集有独立分镜和视频候选，资产池共享。

### P1：角色变体 UI

- 角色卡增加“形态/妆容/服装”列表。
- 支持从角色主图派生变体图。
- 分镜详情中选择出场角色时可选择变体。
- 视频和关键帧生成前显示本镜头实际使用的参考图。

验收：同一角色不同妆容能被人工选择并用于生成；没有选择时按规则回退主图。

### P1：Agent 多集能力

- 决策 Agent 识别“生成第 N 集”“续写下一集”“只做资产”“总结这个故事”等意图。
- 工具上下文增加当前集概览和全集概览。
- 资产 Agent 优先按名称和别名复用全局角色、场景和道具，只有新身份或新形态才创建资产/变体。
- 分镜 Agent 输出 `castRefs`，而不是只输出资产名称。

验收：用户说“生成第 2 集，但沿用第 1 集人物，只给女主换成晚宴妆”，Agent 能写入正确 Episode 和 Variant。

### P2：跨集质量门与批量生产

- 系列一致性检查：已支持未引用项目资产检测、同类型资产重名检测、同类型资产名称/别名冲突检测、本集专属形态未绑定检测、剧集/场景/分镜三级变体适用范围检测、连续场景复用检测、同场景角色形态漂移检测、跨集状态回退检测和资产别名复用，后续继续扩展更复杂的变体覆盖深度和跨集状态变化检查。
- 多集批量生成队列：按集排队，失败集默认保持失败态并跳过，重置后重试；剧集可手动暂缓并从队列恢复；运行中可请求在当前集完成后暂停后续。
- 每集成片后生成制作回顾，回写到剧集状态，供下一集剧本和分镜使用。
- 交付清单已支持旁挂字幕、素材引用表和缺失项报告，后续继续维护导出清单与实际文件的一致性。
- 跨集承接建议已支持批量执行可处理项、基于上一相关剧集状态生成新形态提示词并自动回写推荐变体；当上一相关剧集使用过具体形态而当前集回退主形象时，会生成更明确的状态回退承接建议；当前集还没有分镜出场记录时，也会从最近上一集给出结构化资产/形态承接线索。后续继续扩展更复杂的跨集状态判断。

验收：多集连续生产时，资产复用和状态变化可追踪，失败不会污染其他集；处理续集时可以主动看到上一集回顾和复用形态。

## 关键风险

- 顶层字段和 `episodes[current]` 在过渡期可能双写不一致。P0 必须明确当前阶段的权威来源，并通过 helper 同步。
- 如果只加 UI，不先统一引用 helper，变体仍可能在生成链路里丢失。
- 资产变体不能只靠文本描述；一旦进入关键帧/视频生成，必须能解析出实际 `refImageId`。
- fal、可灵、Veo 等供应商的多参考图能力不同，提示词编号要以实际发送的参考图列表为准。

## 下一步实现顺序

1. 已完成：`Episode` 类型、旧项目迁移、当前集兼容镜像和卡片集数统计。
2. 已完成：`castRefs` 类型与兼容 helper，并改造关键帧、视频参考图和提示词编号链路。
3. 已完成：Episode 切换 UI、原著章节分配、资产变体 UI、连续性详情抽屉。
4. 已完成：Agent 可创建剧集、分配章节、分发章节、复用资产并写入 `castRefs`。
5. 已完成：多集顺序生产入口，按集排队生成资产、关键帧、视频和合成结果，并回写每集成片路径。
6. 已完成：每集成片后生成制作回顾，写入 `Episode.productionRecap`，并在内容变更或手动重置成片状态时清理旧回顾。
7. 已完成：Episode 切换器可重置当前集成片状态，让已成片/失败剧集重新进入全剧生成队列；全剧生成队列会跳过失败集，避免未经确认的自动重跑。
8. 已完成：Episode 切换器可把当前集标记为暂缓/恢复，暂缓集不会参与「生成全剧」。
9. 已完成：全剧生成运行中可请求“暂停后续”，当前集完成后停止继续排队。
10. 已完成：把剧集制作回顾接入主动的跨集承接面板，并通过 Agent 工具 `get_episode_handoff` 暴露给多集续写/换装任务。
11. 已完成：把承接线索升级为自动化建议，辅助补齐参考图、标记变体适用剧集、创建并应用本集形态。
12. 已完成：补全全季导出，可批量复制已成片剧集并生成 `manifest.json`。
13. 已完成：补全“一集一包导出”，可复制当前集成片并生成 `episode.json`。
14. 已完成：增强导出交付报告，单集包和全季清单都会附带素材引用表与缺失项报告。
15. 已完成：补齐字幕交付，单集包和全季导出都会为有字幕内容的剧集生成旁挂 SRT，并在清单登记字幕文件。
16. 已完成：把承接建议深化为批量自动修复和基于上一集状态的提示词生成，可一键执行补图、标记适用范围、新建并应用带提示词的新形态。
17. 已完成：扩展系列一致性质量门，检测同类型角色/场景/道具重名，提示合并或改名以避免同一资产被拆成多份。
18. 已完成：扩展变体覆盖质量门，检测本集已有专属形态但分镜仍用主形象的情况；唯一候选形态可在一致性抽屉中一键绑定，Agent 也会优先调用 `set_storyboard_cast_variant` 修正。
19. 已完成：资产支持别名/昵称/称号；项目搜索、Agent 资产写入和分镜出场匹配会按名称和别名复用既有资产，避免同一身份因称呼不同被重复创建。
20. 已完成：扩展别名冲突质量门，检测同类型资产之间的名称/别名共享，提示合并资产或调整别名，避免 Agent 把同一称呼解析到不同角色、场景或道具。
21. 已完成：扩展场景复用质量门，检测同一 `sceneId` 连续场景漏挂场景资产或混用多个场景资产，提示统一复用同一个场景资产以减少跨镜环境漂移。
22. 已完成：扩展资产复用深度质量门，项目已有分镜后会检测仍未被任何分镜引用的项目级角色/场景/道具资产，提示复用到剧集分镜或清理资产池。
23. 已完成：扩展跨集状态回退质量门，检测上一相关剧集使用过具体形态、后续剧集又回到主形象的分镜，提示确认默认状态恢复或创建/绑定本集形态。
24. 已完成：把跨集状态回退接入承接建议，当前集主形象承接上一相关形态时，会生成更具体的“承接上一形态”建议和本集形态提示词。
25. 已完成：把跨集状态承接约束接入 Agent 生成链路；分阶段分镜和工具循环会读取 `get_episode_handoff/get_continuity_report`，并在续写、换装、妆容、受伤或时期变化场景中优先输出带 `variantLabel/variantId` 的 `castRefs`。
26. 已完成：一致性抽屉的跨集状态回退告警支持一键沿用上一形态；若上一形态已有显式剧集范围，会自动把当前集加入范围并绑定当前分镜。
27. 已完成：空白新集的承接入口和 `get_episode_handoff` 会从最近上一集带出可承接资产/形态线索，但不会在未写分镜前触发自动修复写入。
28. 已完成：扩展同场景角色形态漂移质量门，检测同一 `sceneId` 连续分镜里同一角色混用主形象和不同形态的情况。
29. 已完成：把 `scene_group_variant_mismatch` 写入 Agent 生成/修正规则；除非剧本明确发生换装、化妆、受伤或状态转变，Agent 会优先统一同一 `sceneId` 内的角色形态。
30. 已完成：一致性抽屉的同场景角色形态漂移告警支持一键统一；点击某条漂移告警会把当前集同一 `sceneId` 内该角色统一为该分镜的形态。
31. 已完成：一致性抽屉的连续场景资产告警支持一键补齐漏挂场景资产，或把当前集同一 `sceneId` 内的分镜统一为选中场景资产。
32. 已完成：新增 Agent 工具 `set_storyboard_scene_asset`，可按单个分镜或同一 `sceneId` 场景组补齐/统一场景资产，用于自动修复 `scene_group_missing_asset` 和 `scene_group_asset_mismatch`。
33. 已完成：新增 Agent 工具 `update_asset`，可更新既有资产的名称、别名、描述和提示词；工具循环遇到 `duplicate_asset_name` 或 `duplicate_asset_alias` 时，会优先调整既有资产而不是继续创建重复角色、场景或道具。
34. 已完成：一致性抽屉的别名冲突告警会标出冲突称呼来源；当冲突来自当前资产 aliases 时，可一键移除此别名，避免继续误匹配到多个同类型资产。
35. 已完成：新增 Agent 工具 `set_storyboard_asset_ref`，可把已有角色、场景或道具资产补入指定分镜，也可移除分镜资产引用；工具循环遇到 `unused_project_asset` 且资产应在某集出场时，会优先把它复用到合适分镜，而不是只给出口头建议。
36. 已完成：一致性抽屉的同名资产告警和名称侧别名冲突告警支持直接输入新名称修正，避免为了消除冲突而自动猜测合并或改名。
37. 已完成：一致性抽屉的未引用资产告警支持输入当前集分镜序号，把资产补入该分镜出场引用，给人工复用资产一个安全的确认式快速修复入口。
38. 已完成：变体适用范围质量门从“剧集级”扩展到“剧集/场景/分镜”三级；场景级或分镜级形态会触发 `episode_variant_available` 绑定建议，用错场景或分镜时会提示补充对应适用范围，抽屉修复按钮也会按层级写回 `appliesToEpisodeIds` / `appliesToSceneIds` / `appliesToStoryboardIds`。
39. 已完成：新增 Agent 工具 `set_asset_variant_scope`，可增量追加或移除变体的剧集/场景/分镜适用范围；工具循环遇到 `variant_out_of_episode_scope` 时会优先调用它，避免用 `upsert_asset_variant` 覆盖已有范围数组。
40. 已完成：`episode_variant_available` 会暴露 `candidateVariantIds/candidateVariantLabels`；一致性抽屉在多个本集/本场景/本分镜适用形态同时命中时，可先选择正确候选再绑定，Agent 规则也会要求按剧情选择候选形态。
41. 下一步：继续扩展系列一致性质量门，把更复杂的跨集状态变化纳入自动修复和更细粒度的 Agent 生成约束。
